export const catalogs = {
  mouth: [
    { label: "Узкий", w: 10 },
    { label: "Перекошенный", w: 10 },
    { label: "Широкий", w: 10 },
    { label: "Пухлогубый", w: 10 },
    { label: "Опущенный", w: 7 },
    { label: "Вытянутый вперёд", w: 7 },
    { label: "Сердечко", w: 5 },
    { label: "Кошачий W", w: 5 },
    { label: "Утиный", w: 5 },
    { label: "Крошечный О", w: 4 },
    { label: "Бантик", w: 5 },
    { label: "Волнистый", w: 5 },
    { label: "Квадратный", w: 4 },
    { label: "Боковая ухмылка", w: 5 },
    { label: "Рыбий", w: 4 },
    { label: "Клювообразный", w: 4 },
  ],
  teeth: [
    { label: "Округлые резцы", w: 25 },
    { label: "Мелкие зубки", w: 20 },
    { label: "Редкие зубы", w: 15 },
    { label: "Верхние клыки", w: 20 },
    { label: "Нижние бивни", w: 12 },
    { label: "Акульи зубы", w: 8 },
  ],
  brows: [
    { label: "Мясистые дуги", w: 12 },
    { label: "Тяжёлый надбровный валик", w: 12 },
    { label: "Тонкие брови", w: 10 },
    { label: "Складчатый лоб", w: 8 },
    { label: "Кустистые брови", w: 8 },
    { label: "Монобровь", w: 5 },
    { label: "Злые V", w: 5 },
    { label: "Высокие дуги", w: 5 },
    { label: "Точки", w: 5 },
    { label: "Волнистые", w: 5 },
    { label: "Поникшие", w: 5 },
    { label: "Короткие чёрточки", w: 5 },
    { label: "Спирали", w: 5 },
    { label: "Молнии", w: 5 },
    { label: "Пушистые комочки", w: 5 },
  ],
  eyeCut: [
    { label: "Округлый", w: 12 },
    { label: "Миндальный", w: 12 },
    { label: "Прищур", w: 10 },
    { label: "Опущенные уголки", w: 8 },
    { label: "Косой", w: 8 },
    { label: "Совиный", w: 5 },
    { label: "Треугольный", w: 5 },
    { label: "Полумесяц", w: 5 },
    { label: "Капелька", w: 5 },
    { label: "Лисий", w: 5 },
    { label: "Рыбий", w: 5 },
    { label: "Полуприкрытый", w: 5 },
    { label: "Изумлённый", w: 5 },
    { label: "Сердечко", w: 5 },
    { label: "Прямоугольный", w: 5 },
  ],
  pupil: [
    { label: "Круглый", w: 35 },
    { label: "Вертикальный", w: 25 },
    { label: "Горизонтальный", w: 10 },
    { label: "Козий", w: 10 },
    { label: "Ромбовидный", w: 10 },
    { label: "Волнистый", w: 10 },
  ],
  finish: [
    { label: "Блестящий пластилин", w: 65 },
    { label: "Матовая глина", w: 25 },
    { label: "Перламутр", w: 10 },
  ],
  pattern: [
    { label: "Без рисунка", w: 30 },
    { label: "Леопардовые розетки", w: 12 },
    { label: "Зебровые полосы", w: 12 },
    { label: "Пятнистый", w: 14 },
    { label: "Светлый низ", w: 12 },
    { label: "Узор на спине", w: 10 },
    { label: "Веснушки на булочках", w: 10 },
  ],
  chin: [
    { label: "Пухлый", w: 40 },
    { label: "Вытянутый", w: 28 },
    { label: "Двойной", w: 22 },
    { label: "Косой", w: 10 },
  ],
  horns: [
    { label: "Кривые рожки", w: 10 },
    { label: "Бараньи завитки", w: 9 },
    { label: "Один рог", w: 9 },
    { label: "Без рожек", w: 9 },
    { label: "Коралловые веточки", w: 9 },
    { label: "Мягкие усики", w: 9 },
    { label: "Тупые рожки", w: 9 },
    { label: "Широкий гребень", w: 9 },
    { label: "Загнутые назад", w: 9 },
    { label: "Уплощённые рога", w: 9 },
    { label: "Раздвоенные рога", w: 9 },
  ],
  shape: [
    { label: "Мягкий колобок", w: 10 },
    { label: "Грушевый", w: 9 },
    { label: "Широкий тролль", w: 8 },
    { label: "Высокий лоб", w: 7 },
    { label: "Кривобокий", w: 6 },
    { label: "Двухлобый", w: 5 },
    { label: "Вытянутый череп", w: 5 },
    { label: "Скулы ската", w: 5 },
    { label: "Волнистый", w: 5 },
    { label: "Яйцо", w: 4 },
    { label: "Сердечко", w: 4 },
    { label: "Арахис", w: 4 },
    { label: "Блин", w: 4 },
    { label: "Куб", w: 4 },
    { label: "Капля", w: 4 },
    { label: "Луковица", w: 4 },
    { label: "Капсула", w: 4 },
    { label: "Яблоко", w: 4 },
    { label: "Жёлудь", w: 4 },
  ],
  eyes: [
    { label: "Разноглазый", w: 34 },
    { label: "Сонный хищник", w: 26 },
    { label: "Третий глаз", w: 18 },
    { label: "Циклоп", w: 12 },
    { label: "Паучий взгляд", w: 7 },
    { label: "Глаза на стеблях", w: 3 },
  ],
  nose: [
    { label: "Курносый", w: 5 },
    { label: "Пятачок", w: 5 },
    { label: "Крючковатый", w: 5 },
    { label: "Собачья морда", w: 5 },
    { label: "Нос-картошка", w: 5 },
    { label: "Трубконос", w: 5 },
    { label: "Кошачий нос", w: 5 },
    { label: "Медвежий нос", w: 5 },
    { label: "Морда тюленя", w: 5 },
    { label: "Нос-лист", w: 5 },
    { label: "Без носа", w: 10 },
    { label: "Клюв", w: 4 },
    { label: "Хоботок", w: 4 },
    { label: "Сердечко", w: 4 },
    { label: "Римский", w: 4 },
    { label: "Приплюснутый", w: 4 },
    { label: "Гриб", w: 4 },
    { label: "Звездчатый", w: 4 },
    { label: "Утиный", w: 4 },
    { label: "Раздвоенный", w: 4 },
    { label: "Горошина", w: 4 },
  ],
  ears: [
    { label: "Лесные уши", w: 9 },
    { label: "Длинные зайчьи", w: 9 },
    { label: "Уши летучей мыши", w: 9 },
    { label: "Висячие", w: 9 },
    { label: "Крошечные", w: 8 },
    { label: "Мясистые круглые", w: 8 },
    { label: "Уши-воронки", w: 8 },
    { label: "Складчатые плавники", w: 8 },
    { label: "Кошачьи уши", w: 8 },
    { label: "Мышиные уши", w: 8 },
    { label: "Коровьи уши", w: 8 },
    { label: "Человеческие уши", w: 8 },
  ],
  // Indices 0–10 are the original release; 11–20 append new coats without
  // touching old DNA. Weights of the old block are rescaled by 0.72.
  skin: [
    { label: "Гладкая кожа", w: 9 },
    { label: "Длинный пушок", w: 9 },
    { label: "Кожа с порами", w: 6 },
    { label: "Мягкая кожа", w: 4 },
    { label: "Мягкие дреды", w: 7 },
    { label: "Живые лепестки", w: 7 },
    { label: "Коралловые складки", w: 6 },
    { label: "Шёлковые пряди", w: 5 },
    { label: "Лохматые пучки", w: 6 },
    { label: "Кудри и вихры", w: 7 },
    { label: "Плотная чешуя", w: 6 },
    { label: "Пуховый подшёрсток", w: 3 },
    { label: "Иглы ежа", w: 3 },
    { label: "Мшистые кустики", w: 3 },
    { label: "Перья", w: 3 },
    { label: "Волнистые гребни", w: 3 },
    { label: "Бугорки-шипы", w: 3 },
    { label: "Ленточные локоны", w: 3 },
    { label: "Мохеровые пряди", w: 3 },
    { label: "Круглая чешуя", w: 2 },
    { label: "Кисточки", w: 2 },
  ],
  // Added after DNA v3 shipped: index 0 stays empty so old links look unchanged.
  hair: [
    { label: "Без причёски", w: 40 },
    { label: "Чёлка", w: 6 },
    { label: "Ирокез", w: 6 },
    { label: "Пучок с хвостиком", w: 6 },
    { label: "Кудряшки", w: 6 },
    { label: "Две косы", w: 6 },
    { label: "Пробор набок", w: 6 },
    { label: "Одуванчик", w: 6 },
    { label: "Каре", w: 6 },
    { label: "Антенка", w: 6 },
    { label: "Афро", w: 6 },
  ],
  whiskers: [
    { label: "Без усов", w: 45 },
    { label: "Кошачьи усики", w: 6 },
    { label: "Моржовые", w: 6 },
    { label: "Щёточка", w: 6 },
    { label: "Подкова", w: 6 },
    { label: "Гусарские", w: 5 },
    { label: "Пушистые пёрышки", w: 5 },
    { label: "Сомовьи", w: 5 },
    { label: "Козлиная бородка", w: 6 },
    { label: "Колючая щетина", w: 5 },
    { label: "Пружинки", w: 5 },
  ],
  booty: [
    { label: "Персик", w: 16 },
    { label: "Пухлые булочки", w: 12 },
    { label: "Широкая", w: 10 },
    { label: "Капельки", w: 7 },
    { label: "Высокая", w: 5 },
    { label: "Сердечко", w: 5 },
    { label: "Блин", w: 5 },
    { label: "Крошечные", w: 5 },
    { label: "Огромные", w: 5 },
    { label: "Яблоко", w: 5 },
    { label: "Слитые", w: 5 },
    { label: "Расставленные", w: 5 },
    { label: "Квадратные", w: 5 },
    { label: "Тройные", w: 5 },
    { label: "С ямочками", w: 5 },
  ],
  tail: [
    { label: "Завиток", w: 10 },
    { label: "Короткий крючок", w: 10 },
    { label: "Чертовский", w: 10 },
    { label: "Длинный хвост", w: 10 },
    { label: "Без хвоста", w: 10 },
    { label: "Хвост-лист", w: 10 },
    { label: "Хвост-лопатка", w: 10 },
    { label: "Хвост-плавник", w: 10 },
    { label: "Толстый хвост", w: 10 },
    { label: "Двойной завиток", w: 10 },
  ],
  palette: [
    { label: "Лавандовое молоко", w: 14 },
    { label: "Фисташковый крем", w: 12 },
    { label: "Персиковое суфле", w: 5 },
    { label: "Голубой зефир", w: 7 },
    { label: "Ягодный мусс", w: 10 },
    { label: "Лунное затмение", w: 2 },
    { label: "Жвачка", w: 10 },
    { label: "Кислый лайм", w: 10 },
    { label: "Бирюзовый леденец", w: 9 },
    { label: "Электрический синий", w: 7 },
    { label: "Молочный ирис", w: 8 },
    { label: "Мандариновый мармелад", w: 6 },
  ],
} as const;
export const NOSE_NONE = 10;
export type Trait = keyof typeof catalogs;
export const traitKeys = Object.keys(catalogs) as Trait[];
export type Genome = {
  version: 3;
  seed: string;
  genes: Record<Trait, number>;
  chaos: number;
};
export const palettes = [
  ["#a297d2", "#cfb9e7", "#695792", "#e6aabd", "#a2b766"],
  ["#a7b987", "#d6dcb4", "#657c50", "#dcacac", "#de9f4b"],
  ["#ddaa87", "#f0d0aa", "#a56950", "#da969b", "#94adbd"],
  ["#8eb5c5", "#c2d9d9", "#50778b", "#dcaebf", "#c9a35f"],
  ["#b57f9c", "#deb3c7", "#794b6a", "#d7a09d", "#bacc7f"],
  ["#62637e", "#a8a4bd", "#323344", "#b99ca8", "#d7b65d"],
  ["#ef569a", "#ffaacb", "#902454", "#ff83a9", "#44cbb6"],
  ["#92ce32", "#d5ed80", "#477526", "#d3b750", "#dd7cbb"],
  ["#38b9b0", "#b0e4d3", "#23766f", "#8dc4b1", "#de9c51"],
  ["#536cda", "#b5b8ed", "#303779", "#a68cce", "#dbc153"],
  ["#e2d3bb", "#fff1d6", "#9c7a68", "#d9a7a0", "#5e8f77"],
  ["#e8813b", "#ffc47e", "#944e37", "#e7aa7a", "#749bbc"],
];
export function rng(seed: string) {
  let h = 2166136261;
  for (const c of seed) h = Math.imul(h ^ c.charCodeAt(0), 16777619);
  return () => {
    h += 0x6d2b79f5;
    let t = h;
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
export function generate(seed: string = crypto.randomUUID()): Genome {
  const genes = {} as Genome["genes"];
  for (const k of traitKeys) {
    let n = rng(seed + "/gene/" + k)() * 100;
    genes[k] = catalogs[k].length - 1;
    for (let i = 0; i < catalogs[k].length; i++) {
      n -= catalogs[k][i].w;
      if (n < 0) {
        genes[k] = i;
        break;
      }
    }
  }
  return { version: 3, seed, genes, chaos: 55 };
}
export function validGenome(value: unknown): value is Genome {
  if (!value || typeof value !== "object") return false;
  const g = value as Genome;
  return (
    g.version === 3 &&
    typeof g.seed === "string" &&
    /^[a-zA-Z0-9-]{1,40}$/.test(g.seed) &&
    Number.isFinite(g.chaos) &&
    g.chaos >= 0 &&
    g.chaos <= 100 &&
    !!g.genes &&
    traitKeys.every(
      (k) =>
        Number.isInteger(g.genes[k]) &&
        g.genes[k] >= 0 &&
        g.genes[k] < catalogs[k].length,
    )
  );
}
export function encode(g: Genome) {
  return btoa(
    JSON.stringify({
      version: 3,
      seed: g.seed,
      genes: Object.fromEntries(traitKeys.map((k) => [k, g.genes[k]])),
      chaos: g.chaos,
    }),
  );
}
/** Categories appended after DNA v3 shipped. Their index 0 is the empty variant,
 * so filling them in leaves every old link and saved creature looking the same. */
const appended: Trait[] = ["hair", "whiskers"];
function fillAppended(genes: Record<string, number>) {
  for (const k of appended) genes[k] ??= 0;
  return genes;
}
/** Shared migration for URL DNA and local collections. Never regenerate old categories. */
export function migrateGenome(value: unknown): Genome | null {
  if (!value || typeof value !== "object") return null;
  const v = value as {
    version: number;
    seed: string;
    genes: Record<string, number>;
    chaos: number;
  };
  if (v.version === 3) {
    const result = { ...v, genes: fillAppended({ ...v.genes }) };
    return validGenome(result) ? structuredClone(result) : null;
  }
  if (v.version === 2) {
    const result = {
      ...v,
      version: 3,
      genes: fillAppended({
        ...v.genes,
        pupil: rng(v.seed + "/pupil-v2")() > 0.5 ? 1 : 0,
      }),
    };
    return validGenome(result) ? result : null;
  }
  if (v.version !== 1 || !v.genes || typeof v.genes !== "object") return null;
  const genes = { ...v.genes };
  for (const k of ["mouth", "chin", "horns"]) genes[k] ??= 0;
  const oldMouth = genes.mouth;
  const defaults = generate(v.seed || "migration").genes;
  genes.teeth = [3, 2, 4, 1, 5][oldMouth] ?? 0;
  genes.mouth = [2, 1, 2, 0, 2][oldMouth] ?? 0;
  genes.finish = genes.skin === 3 ? 2 : 0;
  genes.pattern = genes.skin === 2 ? 3 : 0;
  genes.brows = defaults.brows;
  genes.eyeCut = genes.eyes === 1 ? 2 : 0;
  genes.pupil = rng(v.seed + "/pupil-v2")() > 0.5 ? 1 : 0;
  const result = {
    version: 3,
    seed: v.seed,
    genes: fillAppended(genes),
    chaos: v.chaos,
  };
  return validGenome(result) ? result : null;
}
export function decode(s: string): Genome | null {
  try {
    return migrateGenome(JSON.parse(atob(s)));
  } catch {
    return null;
  }
}
/** Shared pupil type; heteromorphism is a rare 1% event, only for two eyes. */
export function pupilTypes(g: Genome, count: number) {
  const r = rng(g.seed + "/pupil-v2"),
    legacyBase = r() > 0.5 ? 1 : 0,
    base = g.genes.pupil ?? legacyBase;
  const different = count === 2 && r() < 0.01;
  const odd = r() < 0.5 ? 0 : 1;
  return Array.from({ length: count }, (_, i) =>
    different && i === odd
      ? base < 2
        ? 1 - base
        : (base + 1 + Math.floor(rng(g.seed + "/odd-pupil")() * 5)) % 6
      : base,
  );
}
export function signature(g: Genome) {
  return encode(g);
}
export function character(g: Genome) {
  const r = rng(g.seed + "name");
  const a = ["Бу", "Мо", "Пу", "Жу", "Фу", "То", "Шу", "Хру", "Ву", "Ню"];
  const b = [
    "блик",
    "мзи",
    "флик",
    "зик",
    "буш",
    "шик",
    "ррик",
    "пси",
    "мба",
    "пуш",
  ];
  return {
    name: a[Math.floor(r() * a.length)] + b[Math.floor(r() * b.length)],
    nature: [
      "Обнимается. Потом кусается.",
      "Коллекционирует потерянные носки.",
      "Боится темноты. Сам светится.",
      "Ворчит, когда его не гладят.",
      "Грозный снаружи. Пудинг внутри.",
      "Съел твой страх. Просит добавки.",
    ][Math.floor(r() * 6)],
    voice: [
      "Бархатный ворчун",
      "Писклявый озорник",
      "Сонный бас",
      "Носатый шептун",
    ][Math.floor(r() * 4)],
    pitch: 0.7 + r() * 0.8,
    rate: 0.8 + r() * 0.3,
  };
}
function surprise(g: Genome) {
  return traitKeys.reduce(
    (v, k) => v - Math.log2(catalogs[k][g.genes[k]].w / 100),
    0,
  );
}
// Reproducible reference population. Tail probability compares the same weighted
// sampler used for new creatures, never local collection size or cosmetic sliders.
const reference = Array.from({ length: 16000 }, (_, i) =>
  surprise(generate("reference-" + i)),
).sort((a, b) => a - b);
export function rarity(g: Genome) {
  const score = surprise(g);
  let lo = 0,
    hi = reference.length;
  while (lo < hi) {
    const mid = (lo + hi) >>> 1;
    if (reference[mid] < score - 1e-9) lo = mid + 1;
    else hi = mid;
  }
  const tail =
    Math.max(1 / reference.length, (reference.length - lo) / reference.length) *
    100;
  return {
    tail,
    score,
    tier:
      tail <= 1
        ? "Легендарный"
        : tail <= 8
          ? "Эпический"
          : tail <= 30
            ? "Редкий"
            : "Необычный",
    color:
      tail <= 1
        ? "#b5842a"
        : tail <= 8
          ? "#8f69b0"
          : tail <= 30
            ? "#568568"
            : "#728092",
    combination: traitKeys.reduce(
      (p, k) => (p * catalogs[k][g.genes[k]].w) / 100,
      1,
    ),
  };
}
