# Existing legacy prototype
RESPEECHER_API_KEY=

# Bulala: server-side credentials (never prefix these with VITE_)
ELEVENLABS_API_KEY=
# Optional default voice. Otherwise create a unique voice in the UI.
ELEVENLABS_VOICE_ID=
