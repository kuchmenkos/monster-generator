# Feedback archive

- `likes/` — monsters the user hearted (JSON + PNG)
- `dislikes/` — monsters the user disliked for agent analysis

Each entry: `{ seed, name, at, processed? }`. Set `processed: true` after fixing the issue that caused the dislike.
